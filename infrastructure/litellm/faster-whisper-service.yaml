apiVersion: v1
kind: Service
metadata:
  name: faster-whisper
  namespace: litellm
  labels:
    app.kubernetes.io/name: faster-whisper
    app.kubernetes.io/instance: faster-whisper
    app.kubernetes.io/component: transcription
    app.kubernetes.io/part-of: litellm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: faster-whisper
    app.kubernetes.io/instance: faster-whisper
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
