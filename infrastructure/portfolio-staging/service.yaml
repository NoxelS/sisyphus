apiVersion: v1
kind: Service
metadata:
  name: portfolio-staging
  namespace: portfolio-staging
  labels:
    app.kubernetes.io/name: portfolio
    app.kubernetes.io/instance: portfolio-staging
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: portfolio
    app.kubernetes.io/instance: portfolio-staging
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
